import styles from './saveLocation.module.css'

const SaveLocation = () => {
    return (
        <form>
            <button className={styles.button}>
                Save Location
            </button>
        </form>
    )
}

export default SaveLocation;